export type ColorKeys = "default" | "primary" | "secondary";

export interface Theme {
  default: {
    backgroundColor: string;
  };
  primary: {
    backgroundColor: string;
  };
  secondary: {
    backgroundColor: string;
  };
}
