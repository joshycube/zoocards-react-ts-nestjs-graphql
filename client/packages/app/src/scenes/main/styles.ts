import styled from "styled-components";

export const Wrapper = styled.div``;

export const ResultColumns = styled.div`
  display: flex;
  justify-content: space-evenly;
  margin-top: ${(props) => props.theme.margins.large};
`;
